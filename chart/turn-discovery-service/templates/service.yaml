apiVersion: v1
kind: Service
metadata:
  name: {{ include "turn-discovery-service.fullname" . }}
  labels:
    {{- include "turn-discovery-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "turn-discovery-service.selectorLabels" . | nindent 4 }}

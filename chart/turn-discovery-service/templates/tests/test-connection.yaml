apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "turn-discovery-service.fullname" . }}-test-connection"
  labels:
    {{- include "turn-discovery-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "turn-discovery-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
